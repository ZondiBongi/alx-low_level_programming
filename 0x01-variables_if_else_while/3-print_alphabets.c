#include <stdio.h>

/** main - Prints the alphabet in lowercase then in uppercaase followed by a new line
 *
 * Return: Always 0 (Success).
 */

int main(void)
{
	char alp[52] = "abcdefghikjlmnopqrstuvwxyABCDEFGHIJKLMNOPQRSTUVXWY"
		int i;
	for(i= 0;i < 52; i++)
	{
		putchar(alph[i]);
	}
	putchar('\n');
	return (0);
}
